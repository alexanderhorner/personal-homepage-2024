// Content for Route / (Index) is contained in _layout.tsx file
// This child route returns null at / so nothing is rendered at <Outlet /> in _layout.tsx 
// <Outlet /> is used for rendering child routes as modals, e.g. /about
export default function Index() {
  return null;
}
